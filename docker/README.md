# Docker Directory

## Purpose
Houses Docker container build manifests and Docker Compose environment orchestrations.

## Contained Modules
- `Dockerfile`: Multi-stage Docker build specification for Flask backend and Node.js frontend asset build.
- `docker-compose.yml`: Local multi-container orchestration with persistent volume mounts (`/data`) and legacy read-only data mounts.

## Dependencies
- `Docker`, `Docker Compose`.

## Important Files
- [`Dockerfile`](file:///C:/Users/Ashish%20Krishna%20Pavan/Desktop/JarvisD/Jarvis-Agent/docker/Dockerfile)
- [`docker-compose.yml`](file:///C:/Users/Ashish%20Krishna%20Pavan/Desktop/JarvisD/Jarvis-Agent/docker/docker-compose.yml)
